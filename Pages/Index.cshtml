@page
@model SteamRater.Pages.SteamIDModel
@{
    ViewData["Title"] = "Steam ID";
}

<h1>Steam Account</h1>

@if (!ModelState.IsValid)
{
    <div class="alert alert-danger">
        @foreach (var error in ModelState.Values.SelectMany(v => v.Errors))
        {
            <p>@error.ErrorMessage</p>
        }
    </div>
}

@{
    var id = Model.Player1.steamid.ToString();

    <p>@id</p>
}



@*

@if (Model.SteamID.steamid.ToString().Equals(""))
{
    <p>No posts found.</p>
}
else
{
   
   
    
        
    
  
    <p>@id</p>
    

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>User ID</th>
                <th>Title</th>
                <th>Body</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var post in Model.Posts)
            {
                <tr>
                    <td>@post.Id</td>
                    <td>@post.UserId</td>
                    <td>@post.Title</td>
                    <td>@post.Body</td>
                </tr>
            }
        </tbody>
    </table>
}
*@